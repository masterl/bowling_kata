#include "bowling.hpp"

size_t bowling( std::string const &scores )
{
    int acc = 0;
    for(auto const& letter : scores)
    {
      
    }
    if(scores.size() > 1) 
    {
      return 4;
    }
    return 3;
}
